import { DevmodeCountdownPipe } from './devmode-countdown.pipe';

describe('DevmodeCountdownPipe', () => {
  it('create an instance', () => {
    const pipe = new DevmodeCountdownPipe();
    expect(pipe).toBeTruthy();
  });
});
