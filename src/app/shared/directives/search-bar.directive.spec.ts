import { SearchBarDirective } from './search-bar.directive';

describe('SearchBarDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchBarDirective();
    expect(directive).toBeTruthy();
  });
});
