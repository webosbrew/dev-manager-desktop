import { SshAuthValueDirective } from './ssh-auth-value.directive';

describe('SshAuthValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SshAuthValueDirective();
    expect(directive).toBeTruthy();
  });
});
